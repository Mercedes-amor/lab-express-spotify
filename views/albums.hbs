<div>

  <div class="containerDiv">
    {{#each this.albumsCollection}}
      <div class="card">
        <img src="{{this.images.0.url}}" alt="carátula" width="300px" />
        <h3>{{this.name}}</h3>
      
        <a href="/tracks/{{this.id}}">
          <button>View Albums</button>
        </a>
      </div>    
    {{/each}}
  </div>
</div>